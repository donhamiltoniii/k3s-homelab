apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ArgoCD points at this path: overlays/homelab
resources:
  - ../../base
  - sealed-secret.yaml        # produced by `kubeseal` — see README

# Example of where per-environment tweaks go. Uncomment to pin the DB PVC to a
# specific node, resize storage, etc., without touching base.
#
# patches:
#   - target:
#       kind: StatefulSet
#       name: supabase-db
#     patch: |-
#       - op: add
#         path: /spec/template/spec/nodeName
#         value: hp-elitedesk-01
